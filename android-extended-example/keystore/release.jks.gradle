android.signingConfigs {
    releaseKeystore {
        storeFile rootProject.file('keystore/release.jks')
        storePassword 'qwerty'
        keyAlias 'release'
        keyPassword 'qwerty'
    }
}
